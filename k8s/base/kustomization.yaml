# Base kustomization for VibeTeam agents
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - product-manager.yaml
  - support-engineer.yaml
  - release-engineer.yaml  # Also contains reliability-engineer
  - software-engineer.yaml
  # Note: github-app-secret.yaml is a template.
  # Create the actual secret manually or via sealed-secrets.
  # Do not include in resources as it contains placeholder values.
