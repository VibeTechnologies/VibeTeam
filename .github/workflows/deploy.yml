name: Build and Deploy

on:
  push:
    branches: [master, main]
    paths:
      - 'vibeteam/**'
      - 'k8s/**'
      - 'Dockerfile'
      - 'pyproject.toml'
      - '.github/workflows/deploy.yml'
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  NAMESPACE: vibeteam

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image_tag: ${{ steps.meta.outputs.tags }}
    steps:
      - uses: actions/checkout@v4

      - name: Set image name lowercase
        id: image_name
        run: echo "IMAGE_NAME=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set image name lowercase
        run: echo "IMAGE_NAME=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Set up kubectl
        uses: azure/setup-kubectl@v3
        with:
          version: 'v1.28.0'

      - name: Configure kubeconfig
        run: |
          mkdir -p ~/.kube
          echo "${{ secrets.KUBECONFIG }}" > ~/.kube/config
          chmod 600 ~/.kube/config

      - name: Verify cluster access
        run: kubectl cluster-info

      - name: Create namespace
        run: kubectl create namespace $NAMESPACE --dry-run=client -o yaml | kubectl apply -f -

      - name: Create secrets
        run: |
          # Create/update ghcr-pull-secret for pulling images
          kubectl -n $NAMESPACE delete secret ghcr-pull-secret --ignore-not-found
          kubectl -n $NAMESPACE create secret docker-registry ghcr-pull-secret \
            --docker-server=ghcr.io \
            --docker-username=${{ github.actor }} \
            --docker-password=${{ secrets.PAT_TOKEN }}

          # Create/update vibeteam-secrets
          kubectl -n $NAMESPACE delete secret vibeteam-secrets --ignore-not-found
          kubectl -n $NAMESPACE create secret generic vibeteam-secrets \
            --from-literal=SENTRY_AUTH_TOKEN="${{ secrets.SENTRY_AUTH_TOKEN }}" \
            --from-literal=GITHUB_TOKEN="${{ secrets.PAT_TOKEN }}" \
            --from-literal=LANGFUSE_PUBLIC_KEY="${{ secrets.LANGFUSE_PUBLIC_KEY }}" \
            --from-literal=LANGFUSE_SECRET_KEY="${{ secrets.LANGFUSE_SECRET_KEY }}" \
            --from-literal=LANGFUSE_BASE_URL="https://langfuse.vibebrowser.app" \
            --from-literal=AZURE_API_KEY="${{ secrets.AZURE_API_KEY }}" \
            --from-literal=AZURE_API_BASE="${{ secrets.AZURE_API_BASE }}" \
            --from-literal=SLACK_SIGNING_SECRET="${{ secrets.SLACK_SIGNING_SECRET }}" \
            --from-literal=SLACK_BOT_TOKEN="${{ secrets.SLACK_BOT_TOKEN }}" \
            --from-literal=GITHUB_WEBHOOK_SECRET="${{ secrets.GITHUB_WEBHOOK_SECRET }}"

          # Create/update slack-bot-secrets (for Slack integration)
          if [ -n "${{ secrets.SLACK_BOT_TOKEN }}" ]; then
            kubectl -n $NAMESPACE delete secret slack-bot-secrets --ignore-not-found
            kubectl -n $NAMESPACE create secret generic slack-bot-secrets \
              --from-literal=SLACK_BOT_TOKEN="${{ secrets.SLACK_BOT_TOKEN }}" \
              --from-literal=SLACK_SIGNING_SECRET="${{ secrets.SLACK_SIGNING_SECRET }}" \
              --from-literal=SLACK_APP_ID="A0AAZGWEAVA"
          fi

          # Create/update gmail-oauth-secrets (if secrets exist)
          if [ -n "${{ secrets.GMAIL_CREDENTIALS_JSON }}" ]; then
            kubectl -n $NAMESPACE delete secret gmail-oauth-secrets --ignore-not-found
            echo '${{ secrets.GMAIL_CREDENTIALS_JSON }}' > /tmp/gmail-credentials.json
            echo '${{ secrets.GMAIL_TOKEN_JSON }}' > /tmp/gmail-token.json
            kubectl -n $NAMESPACE create secret generic gmail-oauth-secrets \
              --from-file=gmail-credentials.json=/tmp/gmail-credentials.json \
              --from-file=gmail-token.json=/tmp/gmail-token.json
            rm -f /tmp/gmail-credentials.json /tmp/gmail-token.json
          fi

      - name: Update image in manifests
        run: |
          # Get the SHA tag
          SHA_TAG="${{ github.sha }}"
          SHORT_SHA="${SHA_TAG:0:7}"
          IMAGE="${{ env.REGISTRY }}/${IMAGE_NAME}:${SHORT_SHA}"
          
          # Update all k8s manifests with new image
          for file in k8s/base/*.yaml k8s/base/webhook/*.yaml; do
            if [ -f "$file" ]; then
              sed -i "s|image: ghcr.io/vibetechnologies/vibeteam:.*|image: ${IMAGE}|g" "$file"
            fi
          done

      - name: Deploy CronJobs
        run: |
          kubectl apply -k k8s/base/ -n $NAMESPACE

      - name: Verify deployment
        run: |
          set +e  # Don't fail on errors
          echo "=== CronJobs ==="
          kubectl get cronjobs -n $NAMESPACE
          echo ""
          echo "=== Secrets ==="
          kubectl get secrets -n $NAMESPACE | grep -E "(gmail|vibeteam|slack)" || echo "No matching secrets"
          echo ""
          echo "=== Recent Jobs ==="
          kubectl get jobs -n $NAMESPACE --sort-by=.metadata.creationTimestamp | tail -5 || echo "No jobs yet"
          echo ""
          echo "=== All Pods ==="
          kubectl get pods -n $NAMESPACE --sort-by=.metadata.creationTimestamp | tail -10 || echo "No pods"
          echo ""
          echo "=== Recent Pod Logs ==="
          # Get logs from the last few pods
          for pod in $(kubectl get pods -n $NAMESPACE --sort-by=.metadata.creationTimestamp -o jsonpath='{.items[*].metadata.name}' | tr ' ' '\n' | tail -5); do
            echo "--- Logs for $pod ---"
            kubectl logs -n $NAMESPACE $pod --tail=30 2>/dev/null || echo "No logs available"
          done
          echo "=== Deployment verification complete ==="
